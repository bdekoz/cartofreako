This source-only GitHub release continues Stage 16 with the completed local
Stage 16I assessment boundary object and Stage 16J Equal Earth comparison
prototype, re-freezes the Stage 15A benchmark input at the 211-artifact
corpus, sources the Marshall Islands cloud plates from the JAXA vendor
pipeline, and advances the orbital-technosphere and Hubble astronomy captures
to 2026-08-13. It does not attach a generated-assets v14 archive and does not
authorize or perform a UCB Active Archive Object Storage deposit over S3.

## Release identity

| Field | Value |
| --- | --- |
| Release tag | `v20260813` |
| Source identity | the immutable commit selected by `v20260813^{commit}` |
| Release kind | GitHub source release |
| Current standard graph | 211 products across 32 pass IDs |
| Re-frozen Stage 15A input | 211 artifacts across 32 pass IDs, 11 layouts, and 14 approved slices at clean commit `7ba10e6b11475326873b3e206067fecebe7cc1c2` |
| Orbital capture | `2026-08-13T15:36:00Z` with the matching NASA SSCWeb query interval |
| Projection runtime | API 3; geometry ABI 1 |
| Generated XZ asset | none |
| UCB AAO / S3 deposit | not invoked; no `cartofreako/v14/` prefix is claimed |

## Stage 16 result

- The Stage 16I integrated outcomes assessment is archived as the canonical
  `reports/cartofreako-audit-outcomes-03.pdf` boundary object with its
  predecessor feedback and next-steps brief. The report uses Devastation
  Pacific v3.9 `assess-outcomes`, `randoma11y-accent`, and the required
  `coda-share` modifier; human review and any sharing remain separate.
- The Stage 16J Equal Earth positioning comparison adds canonical forward and
  reverse equations, a neutral 30-case fixture bundle, diagnostics, and five
  checked local 2560 × 1440 PNGs registered in `make all-experiments`. Equal
  Earth is not in `make all`, the standard artifact catalog, print generation,
  GitHub release, or UCB AAO/S3, and no EPSG:8857 or `#CorrectTheMap`
  compliance claim is made.
- The Stage 15A benchmark input was re-frozen at the 211/32 corpus: 211
  artifacts, 11 layouts, and 14 approved slices at clean commit `7ba10e6`,
  with the runtime API 3 and geometry ABI 1 handoff records, canonical
  parents and SHA-256 values, 1920 × 1080 baselines, content rectangles, and
  parent identities.
- Lossless GPU-control parents are built from the standard graph and the
  Stage 15 contracts are updated to the 211/32 corpus.
- The Marshall Islands speculation plates source their cloud layer from the
  JAXA vendor pipeline and render after the WebAssembly and Natural Earth
  builds.
- Build tooling now provides `make all-experiments-fetch` (clean, then fetch
  every external experimental source), isolates Inkscape D-Bus sessions,
  preserves tracked output evidence in `make clean`, and chains the release
  build through clean, fetch, and experiments.
- The snapshot-dyad marker ledger records the agentic session markers through
  `SD-20260813-e61f77df`; marker metadata only, no authority is granted.

## External data refresh

- Orbital technosphere: CelesTrak OMM snapshots across 16 groups with
  2026-08-13 epochs (active catalog 16,321 objects), the NASA SSCWeb
  reference refreshed for the `2026-08-13T15:30Z–15:40Z` interval, and the
  profile calculation time, query interval, and snapshot collection window
  advanced together to the new capture.
- Astronomy: NASA/JPL Small-Body Database records for 67P, Ceres, Vesta,
  Eros, Apophis, Halley, and Encke were re-fetched with updated SHA-256
  values; the Gaia and exoplanet tables are unchanged. The Hubble observer
  profile follows the science OMM capture so its element set remains within
  the seven-day age contract.

## Verification

- `make check-docs`: passed.
- `make check`: passed, including pass status, projection APIs, print
  contract, artifact selection, the full screen-1080p pass for all 211
  products and 1,055 cataloged files, astro/orbiting generation, and every
  static-data checksum.
- `make check-all-experiments`: passed (8 local builders).
- `git diff --check`: passed before the release commit.

## Publication boundary

GitHub source publication and UCB AAO preservation remain separate concepts
and separate Make targets. This release uses only `release-github`.
`release-ucb-aao-s3` remains top-level, interactive-only, and delegated to
`alpha60-clusterops/bin/load-s3-aao`; it is not a dependency of generation,
experiments, checks, source release, or any unattended agent workflow.
