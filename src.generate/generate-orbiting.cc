// Generate global and observer Orbital Technosphere projections.
// -*- mode: C++ -*-

#include "orbiting-generation.h"

int
main(const int argc, char** argv)
{
  return cart0freak0::orbiting_generation::run(argc, argv);
}
